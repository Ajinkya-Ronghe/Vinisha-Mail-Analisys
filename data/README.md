# Bootstrap dataset

`bootstrap_emails.csv` is a small synthetic dataset included only to make the research prototype runnable. It contains four demonstration labels: `safe`, `threat`, `phishing`, and `malware`.

It must not be used to make thesis accuracy claims. Replace or augment it with documented public datasets, preserve dataset licences, remove duplicates, and keep independent train, validation, temporal, and zero-day test sets.

