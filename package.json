{
  "name": "maildesk",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "vite --host 127.0.0.1 --port 8000",
    "dev:api": ".venv/bin/python -m uvicorn backend.app:app --host 127.0.0.1 --port 8001",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8000"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "vite": "^7.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {}
}
